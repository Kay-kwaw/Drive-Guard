
import 'package:flutter/material.dart';

// Define the background color constant
const Color kBackgroundColor = Color(0xFFEFEFEF);

// Define the app theme constants
final ThemeData kAppTheme = ThemeData(
  primaryColor:Colors.white,
  hintColor: Colors.black, // Replace with your desired font
  // Add more theme configurations as needed
);